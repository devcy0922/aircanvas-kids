# Handoff — 2026-08-25 agents/skills 구축 및 프로젝트 상태 보고

## 1. 이번 세션에서 수행한 작업

1. **프론트엔드 디자인 스킬 세팅**:
   - `https://github.com/anthropics/claude-code/blob/main/plugins/frontend-design/skills/frontend-design/SKILL.md` 표준을 준수하는 `agents/skills/frontend-design/SKILL.md` 생성.
2. **AirCanvas Kids 전용 에이전트 스킬 구축**:
   - `aircanvas-architecture`: v0.2 Phone(Brain)-TV(Thin Display) 아키텍처 및 런타임 데이터 흐름.
   - `aircanvas-protocol`: WebSocket 프로토콜, TVCommand 규약, 0..1 정규화 좌표계, 프로토콜 TS/Rust 동기화 규칙.
   - `aircanvas-calibration-tracking`: MediaPipe 손추적, 4코너 호모그래피 투영 변환, 1-Euro Filter, Pinch 제스처, Zero-Frame 원칙.
   - `aircanvas-tv-engine`: PixiJS 렌더러 기반 씬 렌더링, TVCommandHandler, SVG Path 영역 채색(Hit-Test), 파티클 이펙트.
   - `aircanvas-relay-server`: Rust/Axum + tokio-tungstenite 릴레이 서버, 세션 매칭, 디스커버리(/announce), 포트/네트워크 설정.
3. **규칙 및 백로그 동기화**:
   - `agents/rules.md`에 스킬 가이드 참조 규칙 반영.
   - `.ai/state/task-board.json`에 `TASK-017` 등록 및 완료 처리.
4. **전체 빌드 검증**:
   - `npm run build` (phone, tv, protocol, tv-art) 성공.
   - `cargo check` (server) 성공.

## 2. 프로젝트 현황 요약 (어디까지 진행되었나)

- **완료된 영역 (v0.2 기반 완성)**:
  - **Phone 클라이언트**: MediaPipe 손추적, 4코너 호모그래피 캘리브레이션, 1-Euro 필터, 상태 머신(Lobby→Connecting→Calib→Playing→Gallery), TVCommand 발송 로직, 패키지 매니저(Manifest 다운로드 및 캐시).
  - **TV 클라이언트**: PixiJS 기반 2D 씬 렌더러, `TVCommandHandler` 기반 무상태 커맨드 수신 실행, 테마별(공룡/정글/바다 30종) 아트워크 렌더링.
  - **서버**: Rust/Axum WebSocket 룸 세션 릴레이.
  - **콘텐츠 서버**: Nginx 설정, `manifest.json`, SVG 썸네일.
  - **설계/문서/스킬**: Architecture, Protocol, Calibration, Content Pipeline, Roadmap, Agents Skills 6종 완비.

- **남은 주요 작업 (Pending Backlog)**:
  - `TASK-015`: `fill-at` 좌표 기반 region 판정 커맨드 추가 (현재는 regionId 기반).
  - `TASK-016`: Rust 서버에 `GET /announce` 디스커버리 엔드포인트 구현 (CORS `*` 헤더 포함).
  - `TASK-010`: pinch 제스처 감지 시 TV 브러시 on/off 및 선 그리기 실연동 마무리.
  - `TASK-009`: 폰 카메라 WebRTC 사용을 위한 HTTPS 환경 설정 (로컬 basic-ssl 등).
  - `TASK-008`: 실기기(폰-TV) E2E 연결 및 조명/거리별 추적 안정성 튜닝.
